config BR2_PACKAGE_RGBDS
	bool "rgbds"
	depends on BR2_INSTALL_LIBSTDCPP
	help
	  A z80 compiler

	  https://github.com/rednex/rgbds

comment "RGBDS needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
